# ローカル運用でのログイン導入

1. 管理者アプリを起動する  
2. 初回は `/setup` で管理者のユーザー名・パスワードを決める（ソースには書かない）  
3. 以降は `/login` から入る。職員管理・設定・生成・確定解除・バックアップは管理者のみ  
4. 職員の休み希望ポータル用に、職員管理で 4〜8 桁の PIN を設定する  
5. ポータルでは名前＋PIN。他職員 ID へのアクセスはサーバー側で拒否する  

秘密鍵は DB と同じフォルダの `.auth_secret` に自動生成されます（Git 管理外）。
